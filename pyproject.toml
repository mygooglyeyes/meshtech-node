[project]
name = "meshtech-node"
version = "00.000.020"
description = "Standalone listener + scope feed for the PiMesh radio (meshtech-node, PLAN.md)"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
